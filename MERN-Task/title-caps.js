function toMakeCaps(a){
 for (i=0;i<=a.length;i++){
    console.log("First letter should be cap"  +a[i].charAt(0).toUpperCase()+a[i].substring(1));
    // substring() extracts a part of a string:
 }
}
var names=["burhan","buhari","basith"];
toMakeCaps(names);


// arrow function
var a= () => {
    
    for (i=0;i<=nam.length;i++){
        console.log("First letter should be cap"  + nam[i].charAt(0).toUpperCase()+ nam[i].substring(1));
        // substring() extracts a part of a string:
     }
    }
    var nam=["jennie","bubu","alan"];
   a(nam);